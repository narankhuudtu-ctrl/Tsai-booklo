apply plugin: 'com.android.application'

android {
    namespace 'mn.tsai.booklo'
    compileSdk 34

    defaultConfig {
        applicationId "mn.tsai.booklo"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
